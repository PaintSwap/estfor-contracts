[profile.default]
# Per-contract optimizer overrides were removed ahead of Sonic's Brio hardfork, which raises the
# contract size limit (~90kb). All contracts now compile under the single default profile.
src = "contracts"
test = "test-foundry"
out = "out"
cache_path = "cache-foundry"
libs = ["node_modules", "lib"]
solc = "0.8.28"
evm_version = "cancun"
optimizer = true
optimizer_runs = 9_999_999
via_ir = true

# Promotions owns a game function named testClearPlayerPromotions (an admin op to clear test
# promotions), which Foundry's collector mistakes for a fuzz test. Keep it out of test runs.
no_match_contract = "Promotions"

remappings = [
  "@openzeppelin/contracts/=node_modules/@openzeppelin/contracts/",
  "@openzeppelin/contracts-upgradeable/=node_modules/@openzeppelin/contracts-upgradeable/",
  "@paintswap/=node_modules/@paintswap/",
  "forge-std/=lib/openzeppelin-contracts/lib/forge-std/src/",
]
